// Package mcp implements Leonard's MCP tool surface: verify_symbol,
// find_symbol, list_files, record_decision, get_decisions, record_claim, and
// related tools. Tool handlers are thin shims over package store.
package mcp
