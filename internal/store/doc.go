// Package store wraps the SQLite-backed Leonard data layer: files, symbols,
// decisions, and claims. It is the single source of truth shared by the CLI,
// MCP server, and hook dispatcher.
package store
