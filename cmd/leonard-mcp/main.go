// Command leonard-mcp is the stdio MCP server exposing Leonard's ground-truth
// tools (verify_symbol, find_symbol, record_decision, ...) to Claude Code.
package main

import "fmt"

func main() {
	fmt.Println("leonard-mcp: scaffold — not yet implemented")
}
